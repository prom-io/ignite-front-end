import React from "react";
import {CardHeader, Avatar} from "@material-ui/core";

export const StatusHeader = ({username, displayName, avatar}) => (
    <CardHeader avatar={avatar}
                user
    />
)
